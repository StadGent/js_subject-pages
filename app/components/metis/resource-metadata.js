import MetisResourceMetadataComponent from 'ember-metis/components/metis/resource-metadata';

export default class ResourceMetadataComponent extends MetisResourceMetadataComponent {
  // Inherit all functionality from parent component
  // This provides the custom 't' helper that prefixes translations with 'fallback.' or 'external.'
}
